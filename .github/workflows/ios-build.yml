name: iOS Build and Archive

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Select Xcode version
      run: sudo xcode-select -s /Applications/Xcode_16.2.app

    - name: Clone Feather
      run: |
        git config --global lfs.enabled false
        git clone --recurse-submodules --branch v2.4.0 https://github.com/khcrysalis/Feather.git Feather-v2.4.0

    - name: Show available schemes
      run: |
        xcodebuild -project Feather-v2.4.0/Feather.xcodeproj -list

    - name: Build and Archive iOS App
      run: |
        set -o pipefail
        xcodebuild clean archive \
          -project Feather-v2.4.0/Feather.xcodeproj \
          -scheme Feather \
          -configuration Release \
          -archivePath build/AppMaster.xcarchive \
          -sdk iphoneos \
          -destination 'generic/platform=iOS' \
          -skipPackagePluginValidation \
          -skipMacroValidation \
          -skipPackageUpdates \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGNING_REQUIRED=NO \
          AD_HOC_CODE_SIGNING_ALLOWED=YES \
          ARCHS=arm64 \
          ONLY_ACTIVE_ARCH=NO \
          ENABLE_BITCODE=NO \
          2>&1 | tee build.log | tail -100

    - name: Upload build log on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: build-log
        path: build.log

    - name: Verify archive exists
      run: |
        if [ ! -d "build/AppMaster.xcarchive" ]; then
          echo "❌ Archive not found!"
          exit 1
        fi
        echo "✅ Archive found:"
        ls -la build/AppMaster.xcarchive/Products/Applications/

    - name: Export IPA
      run: |
        mkdir -p build/ipa/Payload
        cp -r build/AppMaster.xcarchive/Products/Applications/*.app build/ipa/Payload/
        cd build/ipa
        zip -r AppMaster.ipa Payload/

    - name: Upload IPA as artifact
      uses: actions/upload-artifact@v4
      with:
        name: AppMaster-IPA
        path: build/ipa/AppMaster.ipa