name: Create New Release

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Xcode Version
        run: |
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Compile Feather
        run: | 
          # تحديث المكتبات الفرعية برمجياً داخل السيرفر لضمان وجودها
          git submodule update --init --recursive
          # تنفيذ أمر البناء الخاص بمشروع Feather
          make
          mkdir -p upload
          # البحث عن ملف الـ IPA ونقله
          find packages -name "*.ipa" -exec mv {} upload/ \; || echo "No IPA found"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: Feather Appmasster Release v${{ github.run_number }}
          tag_name: v${{ github.run_number }}
          files: |
            upload/*.ipa
          generate_release_notes: true
          fail_on_unmatched_files: false
          draft: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          